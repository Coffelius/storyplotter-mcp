# Exclude everything by default; re-include only what the build needs.
# Data is NOT baked into the image — mount it at runtime via STORYPLOTTER_DATA_PATH.
*
!go.mod
!go.sum
!cmd/
!cmd/**
!internal/
!internal/**
!Dockerfile
